# CHANGELOG 编写规则

## 目标读者

面向 AssistsX-Open 的使用者：安装插件、运行自动化脚本的普通用户。

## 写法要求

1. 用日常中文，避免 Room、Bridge、SharedPreferences 等术语；必要时用「数据库」「插件列表」等说法
2. 每条以 **新增**、**修复**、**优化**、**调整** 开头
3. 写用户能感知的变化：  
   - ✅ 新增：安装同名插件时会提示是否覆盖，并展示两个插件的信息  
   - ❌ 新增：`confirmOverwriteIfNeeded` 与 `suspendCancellableCoroutine`
4. 一条只说一件事
5. **版本号**：仅已打包发布的 APK 版本写具体号；开发中内容放在 **待发布**
6. 已发布版本标题格式：`### x.y.z（YYYY-MM-DD）`

## 分类说明

| 分类 | 含义 |
|------|------|
| 新增 | 应用里多了能用的功能 |
| 修复 | 闪退、安装失败、显示错误等问题修好了 |
| 优化 | 操作更顺、界面更清晰、运行更稳 |
| 调整 | 入口增减、默认行为变化 |

## 更新流程

1. 发版前：`git log` 梳理自上一 APK 版本以来的提交
2. 改写成条目，写入 **待发布**
3. 发版后：将 **待发布** 移到 `build.gradle` 中 `versionName` 对应版本下
